package Flowers;

import Enum.*;
import Main_pac.*;

public class Romashka extends Flower {

    public Romashka(boolean fresh, double price, FlowerColor color, double lenght, FlowerType type) {
        super(fresh, price, color, lenght, type);
    }
}
