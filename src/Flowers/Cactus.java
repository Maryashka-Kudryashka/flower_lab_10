package Flowers;

import Enum.*;
import Main_pac.*;

public class Cactus extends Flower {
    public Cactus(boolean fresh, double price, FlowerColor color, double lenght, FlowerType type) {
        super(fresh, price, color, lenght, type);
    }
}
